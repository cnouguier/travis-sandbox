matrix:
  include:
    - os: linux
      dist: trusty
      sudo: required
      jobs:
        include:
          - script:
            - echo 'build'
          - stage: deploy
            script: 'deploy
    - os: osx
      osx_image: xcode7.2 
      script: 
        - echo 'hello ios'
    
